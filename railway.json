{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "echo '=== Starting migrate ===' && python manage.py migrate && echo '=== Migrate complete, starting gunicorn ===' && gunicorn gutendex.wsgi:application --bind 0.0.0.0:8080 --workers 1 --log-level debug --capture-output --enable-stdio-inheritance",
        "healthcheckPath": "/health",
        "restartPolicyType": "ON_FAILURE"
    }
}